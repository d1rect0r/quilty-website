# M1.6 C+D — Phase A Findings Ledger

> Consolidated output of the retroactive 3-agent QA loop across D.3, C.2, D.4 + sprint-wide TypeScript + HIPAA/CSP sweep.
> Each finding has a `Status` field updated as fixes land.
> Severity tiers: `CRITICAL` (security/PHI/correctness), `HIGH` (architecture/canon drift), `MEDIUM` (UX/audit), `LOW` (polish).

---

## D.3 — @quilty/workflow

### Bug-hunter

| ID    | Sev       | File:line                                               | Issue                                                                                                  | Status |
| ----- | --------- | ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | ------ |
| D3-B1 | CRITICAL  | `packages/workflow/src/adapters/in-memory.ts:150-168`   | Unbounded microtask spin in `waitForSignal` if signal never arrives — infinite CPU + leak across tests | FIXED  |
| D3-B2 | CRITICAL  | `packages/workflow/src/adapters/in-memory.ts:308-310`   | `setTimeout` handle in `waitForCompletion` never cleared — handle leak + event-loop hold               | FIXED  |
| D3-B3 | IMPORTANT | `packages/workflow/src/adapters/in-memory.ts:145-148`   | Concurrent `waitForSignal('go')` calls: second consumer gets `undefined`                               | FIXED  |
| D3-B4 | IMPORTANT | `packages/workflow/src/ports/workflow-engine.ts:119`    | `new.target.name` breaks under webpack class-name minification; production error.name becomes `"t"`    | FIXED  |
| D3-B5 | IMPORTANT | `packages/workflow/src/domain/execution-token.ts:14-18` | `ExecutionToken` brand is compile-time only; no `parseExecutionToken` for DynamoDB round-trip          | FIXED  |
| D3-B6 | LOW       | `packages/workflow/src/adapters/in-memory.ts`           | `timed-out` status variant never emitted; `status()` returns `running` after timeout                   | FIXED  |
| D3-B7 | LOW       | `packages/workflow/src/adapters/in-memory.ts:297`       | Missing query handler throws `WorkflowNotFoundError` (semantic mismatch — query gone, not execution)   | FIXED  |
| D3-B8 | LOW       | `packages/workflow/src/ports/workflow-engine.ts:68`     | `cancel(reason)` is unconstrained free-text → PHI sink risk (HIPAA H1 overlap)                         | FIXED  |

### Enterprise canon

| ID    | Sev    | Topic                                                                                     | Status |
| ----- | ------ | ----------------------------------------------------------------------------------------- | ------ |
| D3-E1 | HIGH   | Missing `terminate(token, reason?)` as 7th method distinct from `cancel` (Temporal canon) | FIXED  |
| D3-E2 | HIGH   | No `payloadCodec` slot in `TemporalAdapterOptions` — PHI risk at swap                     | FIXED  |
| D3-E3 | MEDIUM | Missing `executeUpdate` — Temporal Updates GA 2025; flag deferral in ADR                  | FIXED  |
| D3-E4 | MEDIUM | Missing `ctx.condition(predicate, timeoutMs)` — Temporal canon                            | FIXED  |
| D3-E5 | MEDIUM | `terminated` status variant absent (collapsed into `cancelled`)                           | FIXED  |
| D3-E6 | LOW    | Add `startChild` reservation note in ADR for Temporal child-workflow swap                 | FIXED  |

---

## C.2 — k6 load tests + `/api/contact` bypass

### Bug-hunter

| ID    | Sev      | File:line                                            | Issue                                                                                     | Status |
| ----- | -------- | ---------------------------------------------------- | ----------------------------------------------------------------------------------------- | ------ |
| C2-B1 | CRITICAL | `apps/web/app/api/contact/route.ts:74`               | Length early-return breaks constant-time compare — token-length timing oracle             | FIXED  |
| C2-B2 | CRITICAL | `apps/web/app/api/contact/route.ts:69`               | Whitespace-only env token not blocked (`"   "` passes guard)                              | FIXED  |
| C2-B3 | CRITICAL | `tests/load/scenarios/contact-submission.k6.ts:46`   | `fixtures/contact-submissions.json` doesn't exist — scenario crashes at k6 init           | FIXED  |
| C2-B4 | HIGH     | `tests/load/scenarios/contact-submission.k6.ts:64`   | Division-by-zero if fixtures array is empty (`__VU % 0 → NaN`)                            | FIXED  |
| C2-B5 | HIGH     | `apps/web/app/api/contact/route.ts:~250`             | Bypass fires silently — no audit-log entry; D42d CloudWatch trail breaks                  | FIXED  |
| C2-B6 | HIGH     | `tests/load/scenarios/portal-session-chain.k6.ts:43` | `200 or 401` accepts 404 silently; threshold breach inevitable                            | FIXED  |
| C2-B7 | MEDIUM   | `apps/web/app/api/contact/route.ts:53-66`            | WAF allowlist is doc-only, not enforced — operator error leaves bypass internet-reachable | FIXED  |

### Enterprise canon

| ID    | Sev    | Topic                                                                                   | Status |
| ----- | ------ | --------------------------------------------------------------------------------------- | ------ |
| C2-E1 | HIGH   | Missing `static_asset` (edge cache) tier — `static_html` conflates 5-10x latency floors | FIXED  |
| C2-E2 | HIGH   | 30-day warn-only too lax; 14-day post-traffic-baseline is Stripe/Vercel canon           | FIXED  |
| C2-E3 | MEDIUM | Soak should include mixed-workload (80% static / 15% SSR / 4% form / 1% auth) profile   | FIXED  |
| C2-E4 | MEDIUM | `auth_refresh` p99 1200ms too tight for Cognito cold-start (loosen to 1500ms)           | FIXED  |
| C2-E5 | MEDIUM | Fixture PHI assertion: synthetic-domain only (Faker/loadtest.\* sentinel)               | FIXED  |
| C2-E6 | LOW    | Add OG image generation scenario at M2 closure                                          | defer  |

---

## D.4 — Service Worker + iOS coach-mark

### Bug-hunter

| ID    | Sev       | File:line                                                                         | Issue                                                                                                           | Status |
| ----- | --------- | --------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ------ |
| D4-B1 | CRITICAL  | `apps/web/public/sw.js:41`                                                        | Locale regex `[a-z]{2}` misses BCP 47 (`pt-BR`, `zh-Hant`) → PHI account routes silently cacheable              | FIXED  |
| D4-B2 | CRITICAL  | `apps/web/public/sw.js:26` + `packages/security/src/domain/csp-builder.ts:82,119` | CSP `script-src 'self'` blocks Workbox CDN `importScripts` under enforce → entire SW silently degrades to no-op | FIXED  |
| D4-B3 | IMPORTANT | `apps/web/lib/sw/register.ts:38-46`                                               | `clearServiceWorkerCaches()` exported but no invocation site; future sign-out dev may miss the wiring           | FIXED  |
| D4-B4 | IMPORTANT | `apps/web/lib/sw/register.ts:26-29`                                               | SW registration errors swallowed — `window.onerror` does not capture Promise rejections in event listeners      | FIXED  |
| D4-B5 | IMPORTANT | `apps/web/components/site/IOSCoachMark.tsx:38-69`                                 | `<dialog open>` (non-modal) — no focus trap, Tab escapes the modal                                              | FIXED  |
| D4-B6 | IMPORTANT | `apps/web/components/site/InstallPrompt.tsx:53-54,69`                             | `detectPlatform()` in `useState` lazy initialiser → SSR/client hydration mismatch in React 19                   | FIXED  |

### Enterprise canon

| ID    | Sev    | Topic                                                                                   | Status             |
| ----- | ------ | --------------------------------------------------------------------------------------- | ------------------ |
| D4-E1 | HIGH   | Self-host Workbox (vendor `workbox-sw.js` to `/workbox/`); resolves D4-B2 + CSP cleanup | FIXED (PB-1)       |
| D4-E2 | HIGH   | Add `request.method !== 'GET'` guard inside `isExcluded()` (server actions are POST)    | FIXED (PB-1)       |
| D4-E3 | MEDIUM | Engagement gate: don't show prompt on first paint; require Nth visit + scroll signal    | defer (flag-gated) |
| D4-E4 | MEDIUM | iOS coach-mark copy out of date — iOS 26 hides Share behind `•••` menu                  | FIXED (PB-5)       |
| D4-E5 | MEDIUM | Persist dismissal in localStorage with 30-day TTL                                       | FIXED (PB-5)       |
| D4-E6 | MEDIUM | `LOGOUT_CLEAR_CACHES` should also `registration.unregister()` + hard reload             | FIXED (PB-5)       |
| D4-E7 | LOW    | StaleWhileRevalidate for images (`request.destination === 'image'`)                     | defer              |

---

## Sprint-wide TypeScript sweep

| ID   | Sev    | File:line                                               | Issue                                                                                                | Status |
| ---- | ------ | ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ------ |
| TS-1 | HIGH   | `packages/workflow/src/adapters/in-memory.ts:299`       | `handler() as never` → type lie; bypasses narrowing                                                  | FIXED  |
| TS-2 | HIGH   | `packages/workflow/src/adapters/in-memory.ts:147,162`   | `queue.shift() as T` widens `undefined → T`                                                          | FIXED  |
| TS-3 | HIGH   | `packages/search/tsconfig.json:9-10`                    | `__tests__` excluded from typecheck → search tests are TS-blind                                      | FIXED  |
| TS-4 | MEDIUM | `apps/web/components/site/InstallPrompt.tsx:85-88`      | Install button async onClick has no try/catch — `prompt()` DOMException leaks as unhandled rejection | FIXED  |
| TS-5 | MEDIUM | `apps/web/components/site/InstallPrompt.tsx:61`         | `e as BeforeInstallPromptEvent` unchecked cast                                                       | FIXED  |
| TS-6 | MEDIUM | `packages/workflow/src/adapters/in-memory.ts:307,313`   | `as Promise<never>` cast wider than `as Promise<TOutput>`                                            | FIXED  |
| TS-7 | LOW    | `packages/workflow/src/domain/execution-token.ts:30-32` | Brand round-trip undocumented (overlap with D3-B5)                                                   | FIXED  |
| TS-8 | LOW    | `packages/search/src/index.ts:45-49`                    | Algolia skeleton re-exported from production barrel (systemic; same in workflow)                     | defer  |

---

## Sprint-wide HIPAA / CSP sweep

| ID     | Sev      | File:line                                                                     | Issue                                                                                                                                      | Status |
| ------ | -------- | ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| HIP-C1 | CRITICAL | `apps/web/public/sw.js:26` + `csp-builder.ts:82,119`                          | (Duplicate of D4-B2) Workbox CDN load violates CSP                                                                                         | FIXED  |
| HIP-C2 | CRITICAL | `apps/web/components/site/InstallPrompt.tsx:87`                               | Lying JSDoc: "logs outcome to analytics chokepoint" — code drops `userChoice`. Future dev may add `analytics.track()` without consent gate | FIXED  |
| HIP-H1 | HIGH     | `packages/workflow/src/ports/workflow-engine.ts:71` + `workflow-status.ts:29` | (Same as D3-B8) `cancel(reason)` unconstrained free-text → CloudWatch PHI sink. Restrict to closed enum.                                   | FIXED  |
| HIP-H2 | HIGH     | `apps/web/public/sw.js:41`                                                    | (Same as D4-B1) SW locale regex misses BCP 47                                                                                              | FIXED  |
| HIP-H3 | HIGH     | `tests/load/scenarios/contact-submission.k6.ts:40-47`                         | No synthetic-domain assertion on fixture emails → accidental prod-data load sends real users acknowledgement emails from k6                | FIXED  |
| HIP-M1 | MEDIUM   | `apps/web/app/api/contact/route.ts:74`                                        | (Same as C2-B1) Constant-time compare leaks length                                                                                         | FIXED  |
| HIP-M2 | MEDIUM   | `packages/security/src/domain/csp-builder.ts:29,46`                           | Portal `connect-src` permits any `*.ingest.us.sentry.io` subdomain — pin to project DSN before portal goes live                            | FIXED  |
| HIP-M3 | MEDIUM   | `apps/web/components/site/InstallPrompt.tsx:86-88`                            | Native `'dismissed'` outcome doesn't trigger `setDismissed(true)` — prompt re-renders after dismiss                                        | FIXED  |
| HIP-L1 | LOW      | `apps/web/app/api/contact/route.ts:250-251`                                   | (Same as C2-B5) Bypass-fired audit log entry missing                                                                                       | FIXED  |
| HIP-L2 | LOW      | `packages/tokens/dist/flutter/lib/src/colors.dart`                            | Confirmed PHI-clean — no action.                                                                                                           | n/a    |
| HIP-L3 | LOW      | `packages/search/src/adapters/algolia.ts:38-43`                               | `proxyUrl?` typed optional but comment says required at activation                                                                         | defer  |

---

## Aggregation: actionable items (de-duplicated, prioritized)

The unique work items (collapsing overlaps like D3-B8 ↔ HIP-H1, C2-B1 ↔ HIP-M1, etc.):

### Phase B fixes (this round)

1. **Constant-time compare hardening** (C2-B1, HIP-M1) — use `timingSafeEqual` + length-equal pad
2. **Whitespace token guard** (C2-B2) — `!envToken?.trim()`
3. **Bypass audit log** (C2-B5, HIP-L1) — `container.logger.info('contact_form_rate_limit_bypassed', {...})`
4. **SW locale regex BCP 47** (D4-B1, HIP-H2) — `/^\/[a-z]{2,}(-[a-zA-Z0-9]+)*\/account\//`
5. **Self-host Workbox** (D4-B2, D4-E1, HIP-C1) — vendor `workbox-sw.js` into `/public/workbox/`
6. **SW non-GET guard** (D4-E2) — `request.method !== 'GET' → NetworkOnly`
7. **waitForSignal push model** (D3-B1, D3-B3, TS-2) — `Map<string, Array<resolver>>`, eliminate microtask polling
8. **waitForCompletion clearTimeout** (D3-B2) — store handle, clear in finally
9. **WorkflowEngineError name string literals** (D3-B4) — explicit assignment, not `new.target.name`
10. **`as TOutput` / `as TResult`** (TS-1, TS-6) — fix the `as never` lies
11. **`shift()` undefined check** (TS-2) — narrow before resolve
12. **Add `terminate` method + `terminated` state** (D3-E1, D3-E5) — 7th port method
13. **Restrict `cancel(reason)` to enum** (D3-B8, HIP-H1) — closed union, not free string
14. **Add `payloadCodec` to TemporalAdapterOptions** (D3-E2) — pre-empty PHI risk
15. **@quilty/search tsconfig include **tests\*\*\*\* (TS-3) — single-line fix
16. **IOSCoachMark showModal + focus trap** (D4-B5) — useRef + dialog.showModal()
17. **InstallPrompt hydration fix** (D4-B6) — null-first, set in useEffect
18. **InstallPrompt try/catch + outcome handling + comment fix** (TS-4, TS-5, HIP-C2, HIP-M3) — fix lying comment, gate userChoice, run-time guard on event
19. **k6 fixture file scaffold + synthetic-domain guard** (C2-B3, C2-B4, C2-E5, HIP-H3) — seed file + email-domain assertion
20. **portal-session-chain status check fix** (C2-B6) — accept 404 explicitly until M6
21. **Add static_asset tier to thresholds** (C2-E1) — split edge-cache from origin
22. **Tighten warn-only to 14 days** (C2-E2) — README + watchlist
23. **Loosen `auth_refresh` p99 to 1500ms** (C2-E4) — Cognito cold-start margin
24. **register.ts Sentry capture** (D4-B4) — wire to container.errorReporter
25. **clearServiceWorkerCaches TODO + ADR cross-ref** (D4-B3) — comment + ADR-0022 ref
26. **localStorage dismissal for InstallPrompt** (D4-E5) — 30-day TTL
27. **LOGOUT_CLEAR_CACHES unregister + reload** (D4-E6) — registration.unregister() + location.reload()
28. **iOS 26 coach-mark copy update** (D4-E4) — `•••` ellipsis menu
29. **Sentry connect-src pin to project DSN** (HIP-M2) — narrow from wildcard
30. **`parseExecutionToken` validator** (D3-B5, TS-7) — runtime brand round-trip
31. **timed-out status emission** (D3-B6) — set status before timeout reject
32. **WorkflowQueryNotFoundError error type** (D3-B7) — distinct from WorkflowNotFoundError
33. **ADR-0021 amendments** — terminate, payload codec, executeUpdate deferral, startChild reservation, mixed-workload soak
34. **ADR-0022 amendments** — self-host Workbox decision + LOGOUT_CLEAR_CACHES protocol + iOS 26 + engagement-gate deferral
35. **Verification report append** — capture this fix cycle

### Deferred (explicit non-action with reason)

- D3-E6 (`startChild`) — reserved in ADR, defer to Temporal swap
- C2-E6 (OG image scenario) — defer to M2
- D4-E3 (engagement gate) — defensible since flag-gated; document in ADR
- D4-E7 (StaleWhileRevalidate for images) — defer to first-image MDX
- TS-8 (skeleton in barrel) — systemic; raise in next refactor sprint
- HIP-L3 (Algolia `proxyUrl` optional) — defer; revisit at activation

---

## Fix sequence (commits)

1. **`fix(security/cd-phase-b): constant-time compare + SW locale + Workbox self-host`** — items 1-6 (security boundary)
2. **`fix(workflow/cd-phase-b): in-memory fake correctness + WorkflowEngineError shape`** — items 7-11, 30, 31, 32
3. **`feat(workflow/cd-phase-b): terminate method + cancel reason enum + payload codec`** — items 12-14
4. **`fix(tokens-search/cd-phase-b): tsconfig include __tests__`** — item 15
5. **`fix(sw-prompt/cd-phase-b): IOSCoachMark modal + InstallPrompt hydration + register sentry`** — items 16-18, 24-28
6. **`fix(c2/cd-phase-b): k6 fixtures synthetic + thresholds tiers + portal-session status`** — items 19-23
7. **`fix(security/cd-phase-b): Sentry connect-src pin`** — item 29
8. **`docs(m1.6): ADR-0021 + 0022 amendments + verification report append`** — items 33-35

---

## Phase-C retroactive sweep (post-PB-8)

Phase-C 4-agent sweep (typescript-reviewer + hipaa-csp-reviewer +
perf-bundle-reviewer + accessibility-reviewer) ran across the 8
Phase-B commits. Summary of items closed:

### Phase-C CRITICAL (PB-9)

| Tag      | Topic                                                        | Status       |
| -------- | ------------------------------------------------------------ | ------------ |
| HIPAA-C1 | `WorkflowNotFoundError` interpolated open-string signal name | FIXED (PB-9) |
| HIPAA-C2 | `WorkflowCancellationError.message` interpolated `reason`    | FIXED (PB-9) |
| Perf-C   | `/workbox/*` served without long-cache headers               | FIXED (PB-9) |
| A11y-C   | Buttons fail WCAG 2.5.5 (44x44 minimum target size)          | FIXED (PB-9) |

### Phase-C WARNINGS (PB-9 + A-series)

| Tag      | Topic                                                                                     | Status           |
| -------- | ----------------------------------------------------------------------------------------- | ---------------- |
| HIPAA-W1 | `WorkflowCancelReason` runtime validator + cast-loophole guard                            | FIXED (PB-9)     |
| HIPAA-W2 | `pinnedSentryHostOrNull` regex tightened to `o[0-9]+` + region note                       | FIXED (PB-9)     |
| HIPAA-W3 | Sentry SDK init gated on CSP-coherent DSN host (no init = no CSP-violation feedback loop) | FIXED (A1)       |
| HIPAA-W4 | SW LOGOUT_CACHES_CLEARED order reversed (postMessage before unregister)                   | FIXED (PB-9)     |
| HIPAA-W5 | localStorage key format spec divergence (cosmetic; no PHI surface)                        | accepted (no-op) |
| HIPAA-W6 | IOSCoachMark explicit focus-restore on close                                              | FIXED (PB-9)     |
| TS-W1    | `WorkflowTerminationError` is now a proper subclass                                       | FIXED (PB-9)     |
| TS-W2    | `WorkflowCancelReason` re-exported from `/testing` subpath                                | FIXED (PB-9)     |
| TS-W3    | `WorkflowCancelReason` runtime validator (closes `as` cast loophole)                      | FIXED (PB-9)     |
| TS-W4    | InstallPrompt dismissal store uses `storage`-event subscribe                              | FIXED (PB-9)     |
| TS-W5    | `getExecutionState` shallow-readonly hazard → deep-frozen snapshot type                   | FIXED (A2)       |
| Perf-W1  | `vendor-workbox.mjs` filters `.dev.js` + `.map` (22 files → 6)                            | FIXED (PB-9)     |
| Perf-W2  | `register.ts` dynamic-imports composition.client on cold error path                       | FIXED (PB-9)     |
| Perf-W3  | `static_asset` threshold doc attribution updated to CloudFront/SST                        | FIXED (A7)       |
| A11y-W1  | InstallPrompt `<section>` uses `aria-labelledby` pointing to in-section `<h2>`            | FIXED (PB-9)     |
| A11y-W2  | IOSCoachMark docstring corrected (focus lands on button, not heading)                     | FIXED (PB-9)     |

### A-series additional polish (post Phase-C)

| Tag | Topic                                                                        | Status |
| --- | ---------------------------------------------------------------------------- | ------ |
| A4  | `instanceof WorkflowCancellationError` regression test for terminated error  | FIXED  |
| A5  | This ledger's status column refreshed to reflect closures                    | FIXED  |
| A3  | `pnpm size` re-verified post-PB-9                                            | FIXED  |
| A6  | Full `apps/web build` smoke (vendor-workbox filter + tokens + SW deployable) | FIXED  |
| A8  | axe-core a11y test for `InstallPrompt` + `IOSCoachMark`                      | FIXED  |
