# M1.5 Sprint Plan

> **Scope:** 42 atomic commits across 6 sub-waves. ~10-13 working days realistic. Spans 2-3 working sessions with QA loops between commits. Same per-commit QA discipline as M1.
>
> **Source decisions:** `decisions-log.md` (D75-D195, META-1-5 + revisions to D6/D9/D11/D45/D52/D63/D67/D68 + A.1-A.3 corrections + Section C answers).
>
> **Sprint boundary:** Push held until final QA + user authorization, same as M1.
>
> **Plan revision history:** v2 (audit-revised) applied 3-agent audit findings — completeness (7 HIGH on missing runbooks + sst.config/instrumentation re-wires), sequencing (6 HIGH on ordering + caller enumeration + commit duplication), compliance (6 HIGH on default-deny chokepoint semantics + ESLint scope + D-number correction + sub-processor opt-in form). Key structural changes from v1: (a) Wave 1 commit 4 ↔ commit 5 swap so `@quilty/security` Sanitizer port exists before `@quilty/observability` factory wrappers consume it per D67, (b) Wave 4 commit 27 split into 27a/27b/27c for atomic discipline across 4 legal pages, (c) net total +2 commits → 42.

---

## Sub-wave structure

| Wave       | Theme                              | Commits | Approx duration | Description                                                                                                   |
| ---------- | ---------------------------------- | ------- | --------------- | ------------------------------------------------------------------------------------------------------------- |
| **Wave 1** | Foundation refactor                | 11      | 3-4 days        | Modular monolith + hexagonal extraction; no new features                                                      |
| **Wave 2** | Production bug fixes               | 5       | 1 day           | Manifest icons, AASA, tag drift + 6yr log retention, WCAG error pages, strategy doc revisions                 |
| **Wave 3** | Technical routes                   | 5       | 1 day           | security.txt, change-password, gpc.json, manifest depth, robots refinement                                    |
| **Wave 4** | Legal + compliance pages           | 9       | 2-3 days        | Cookie banner, DSAR pages (3 commits), /trust, accessibility statement, sub-processor list, ESLint discipline |
| **Wave 5** | Forms + error handling             | 4       | 1-2 days        | Canonical form pattern + /contact + per-group error boundaries + PHI ESLint                                   |
| **Wave 6** | Cleanup + ADRs + QA + verification | 8       | 1-2 days        | Final QA sweep, ADR batch, strategy doc batch, verification report                                            |

**Total commits:** 42
**Realistic wall-clock:** 10-13 working days across 2-3 sessions
**Agent invocations (estimate):** 42 × 3 per-commit + 6 final-QA = ~132

---

## Wave 1 — Foundation refactor (11 commits)

> The bones of the modular monolith. Every subsequent wave builds on this. No new user-facing features ship in Wave 1 — pure architectural extraction.

### Commit 1 — `chore(arch): bootstrap pnpm workspaces + Turborepo + scoped ESLint adapter chokepoint + scoped dep-cruiser graph rules`

**Files:**

- `pnpm-workspace.yaml` — declare `apps/*`, `packages/*` globs (already exists; verify)
- `turbo.json` — pipelines for new packages: lint, typecheck, test, build per package
- `tsconfig.base.json` — composite + references-aware
- `eslint.config.mjs` — **scoped** `no-restricted-imports` rule: NEW rule fires only on files under `packages/**` paths (D78 narrow); EXISTING M1 vendor-SDK rule for `apps/web/lib/observability/**` left in place until commits 5 (security) and 4 (observability) replace it. Net effect: rules tighten as each `apps/web/lib/*` directory is deleted; never false-positive on intermediate state.
- `.dependency-cruiser.cjs` — 5 graph rules per D79, scoped so M1 baseline passes:
  1. No cycles (global)
  2. No `domain/` → `adapters/` (global; M1 has neither, fires only after Wave 1 commits 4+ ship them)
  3. Cross-package imports allowlisted to `^packages/[^/]+/src/(index|__fakes__/index)\\.ts$` only (covers both the public barrel and the `./testing` subpath barrel that consumer tests need)
  4. `apps/web/**` cannot import `@quilty/<role>/src/adapters/<vendor>` directly — only the package barrel
  5. Utility packages (`@quilty/seo`, `@quilty/content`, `@quilty/shared-types`) exempt from ports/adapters rules
- `knip.json` — updated for new package globs

**Decisions referenced:** D75-D81 (architecture), D78, D79

**Pre-conditions:** None (M1 + M1+1 baseline)

**Post-conditions:**

- `pnpm install` succeeds with no packages-yet
- ESLint passes — both old M1 vendor-SDK allowlist (for `apps/web/lib/observability/*`) AND new packages-scoped rule coexist
- dep-cruiser passes (no new files violate)
- All M1 tests still green (no regressions; refactor hasn't started)

**Per-commit QA:**

- Pass A: typescript-reviewer + perf-bundle-reviewer (workspace configs)
- Pass B: sst-iac-reviewer (verify SST config compatibility)

### Commit 2 — `chore(tooling): turbo generator suite (5 templates)`

**Files:**

- `turbo/generators/config.ts` — Plop config wiring 5 generators
- `turbo/generators/templates/package/` — `{{ name }}/package.json.hbs`, `tsconfig.json.hbs`, `src/index.ts.hbs`, `src/ports.ts.hbs`, `src/errors.ts.hbs`, `README.md.hbs`
- `turbo/generators/templates/port/` — `ports-snippet.ts.hbs`, `__tests__/{{name}}.contract.test.ts.hbs`, `__fakes__/{{name}}.fake.ts.hbs`
- `turbo/generators/templates/adapter/` — `adapters/{{vendor}}.ts.hbs`, `adapters/{{vendor}}.test.ts.hbs`
- `turbo/generators/templates/fake/` — `adapters/in-memory.ts.hbs`
- `turbo/generators/templates/utility-package/` — utility-package layout (no ports/adapters subtree)
- Auto-updates: Knip workspace entry, dependency-cruiser allowlist, size-limit budget for client-shipped packages

**Decisions referenced:** META-5 (5 generators)

**Per-commit QA:** Pass A: typescript-reviewer (template type-safety) + sst-iac-reviewer; Pass B: perf-bundle-reviewer

### Commit 3 — `chore(arch): composition root scaffold + globalThis anchor + makeContainer factory + ADR-0010 stub`

**Files:**

- `apps/web/composition.server.ts` — server-runtime composition root
- `apps/web/composition.client.ts` — client-runtime composition root (browser-side)
- `apps/web/composition.edge.ts` — Edge-runtime composition root (proxy.ts consumer)
- `apps/web/lib/get-container.ts` — `getContainer(env)` accessor with `globalThis.__quiltyContainer` anchor (Next.js 16 webpack chunk-duplication mitigation — see ADR-0010 for rationale)
- `docs/adr/0010-composition-root.md` stub (filled at Wave 6)

**META-2 note:** All file-content comments reference ADR-0010 / D77, never workflow context (no `// per Round-6 audit`, `// fix from agent 17`, etc.).

**Decisions referenced:** D77 (composition root pattern)

**Per-commit QA:** Pass A: typescript-reviewer + hipaa-csp-reviewer (no premature vendor imports); Pass B: perf-bundle-reviewer

### Commit 4 — `refactor(security): extract apps/web/lib/security → @quilty/security hexagonal package (Sanitizer-first; chokepoint primitive for downstream packages)`

**Ordering rationale:** Security extraction lands BEFORE observability because D67 requires the PHI Sanitizer port to be the architectural chokepoint that observability's factory wrappers call into. `@quilty/observability` (Commit 5) cannot satisfy D67 architecturally if `@quilty/security`'s `Sanitizer` port doesn't yet exist.

**Files:**

- `packages/security/package.json` — `@quilty/security` (4 enumerated ports + 3 forms-canonical domain utilities)
- `packages/security/src/ports.ts` — D91 Sanitizer + D92 RedirectValidator + D93 CspBuilder + D94 HeadersBuilder + D95 Result envelope
- `packages/security/src/domain/sanitizer.ts` — PHI denylist + JWT regex + UUID hash + **recursive string-value traversal** (any object value at any depth whose key matches the denylist OR whose value shape-matches PHI regex gets redacted). Architectural seal: not just a top-level key check.
- `packages/security/src/domain/redirect-validator.ts` — open-redirect defense (**D92 RedirectValidator port** — NOT D122 as v1 of plan mislabeled; D122 in the locked log is the marketing double-opt-in decision)
- `packages/security/src/domain/csp-builder.ts` — two-tier CSP builder (D59)
- `packages/security/src/domain/headers-builder.ts` — HSTS phased ramp (D60)
- **`packages/security/src/domain/csrf.ts` — STUB (filled at Commit 32 per D113):** export typed `verifyCsrf()` + `generateCsrfToken()` signatures that throw `NotImplementedError` at runtime; tests assert signature shape only. This pre-declaration prevents Commit 32 from retroactively expanding a "completed" package surface.
- **`packages/security/src/domain/honeypot.ts` — STUB (filled at Commit 32 per D113):** typed signatures only.
- **`packages/security/src/domain/time-trap.ts` — STUB (filled at Commit 32 per D113):** typed signatures only.
- `packages/security/src/__fakes__/*.fake.ts`
- `packages/security/src/__tests__/*.contract.test.ts` — including a Sanitizer contract test that submits an object with PHI-shaped values at depth 3+ and asserts redaction
- `packages/security/index.ts` + `packages/security/src/__fakes__/index.ts` — barrel exports for dep-cruiser allowlist
- **Caller enumeration (all M1 consumers re-wired in this commit):**
  - `apps/web/proxy.ts` — wire `@quilty/security` factories (CspBuilder + HeadersBuilder + Sanitizer)
  - `apps/web/instrumentation.ts` — replace `import { isSensitiveKey } from '@/lib/observability/sanitize'` with `import { Sanitizer } from '@quilty/security'`
  - `apps/web/lib/security/__tests__/*` — DELETE (moved to package)
  - `apps/web/lib/security/*` — DELETE
- `apps/web/composition.*.ts` — wire 4 security ports

**Decisions referenced:** D75-D81, D91-D95, D67 PHI chokepoint (architectural recursion), D92 RedirectValidator, META-1 (vendor-agnostic naming), META-2 (comment scope)

**Per-commit QA:** Pass A: typescript-reviewer + hipaa-csp-reviewer (recursive sanitizer correctness + chokepoint primitive shape); Pass B: sst-iac-reviewer (proxy.ts runtime compat)

### Commit 5 — `refactor(observability): extract apps/web/lib/observability → @quilty/observability hexagonal package (consumes @quilty/security Sanitizer)`

**Files:**

- `packages/observability/package.json` — `@quilty/observability` + exports map (`.`, `./testing`, `./amplitude`, `./sentry`, `./cloudwatch`, `./sentry-replay`, `./env-flags`)
- `packages/observability/tsconfig.json`
- `packages/observability/src/ports.ts` — D82 Analytics + D83 ErrorReporter + D84 Logger + D85 Replay + D86 FeatureFlagEvaluator interfaces; `REPLAY_BLOCK_CLASS` + `REPLAY_MASK_CLASS` constants; `AccountDeleteReason` enum ships with **M1 baseline values** unchanged; D137 expansion to 8 values lands at Commit 33 to keep the change atomic and traceable.
- `packages/observability/src/errors.ts` — typed error unions per port
- `packages/observability/src/domain/` — factory wrappers applying cross-cutting. **Default-deny architectural seal:**
  - `wrapAnalytics(adapter, consentStore, sanitizer)` returns a no-op `Analytics` if `consentStore.get(id)` is `null` OR `state.analytics !== true`. Contract test asserts the no-op behavior and asserts that a "raw adapter bypass" attempt (importing `./adapters/amplitude` from outside the package) is blocked by the ESLint rule from Commit 1.
  - `wrapErrorReporter(adapter, sanitizer)` calls `sanitizer.scrub()` on every event payload before delegating.
  - `wrapLogger(adapter, sanitizer)` same.
  - `wrapReplay(adapter, config)` enforces per-config mask/block floor (D68 per-config replay discipline).
- `packages/observability/src/adapters/amplitude.ts` — Amplitude analytics adapter
- `packages/observability/src/adapters/sentry.ts` — Sentry error-reporter adapter
- `packages/observability/src/adapters/cloudwatch.ts` — structured-JSON logger adapter
- `packages/observability/src/adapters/sentry-replay.ts` — Sentry Replay adapter
- `packages/observability/src/adapters/env-flags.ts` — env-var feature-flag reader
- `packages/observability/src/adapters/in-memory.ts` — fake for tests + local dev (5 ports' fakes)
- `packages/observability/src/__fakes__/*.fake.ts` — exported via `./testing` subpath
- `packages/observability/src/__tests__/*.contract.test.ts` — parameterized over adapter factories via `describe.each`; includes default-deny test + "no analytics emission before consent" test + "raw adapter bypass throws" test (mocks an unwrapped adapter and asserts it does not pass sanitizer)
- `packages/observability/README.md` — port docs + adapter substitution path docs
- `packages/observability/index.ts` + `packages/observability/src/__fakes__/index.ts` — barrel exports
- **Caller enumeration (all M1 consumers re-wired in this commit):**
  - `apps/web/sentry.client.config.ts` — re-wire to `@quilty/observability/sentry` factory
  - `apps/web/sentry.server.config.ts` — same
  - `apps/web/sentry.edge.config.ts` — same
  - `apps/web/instrumentation.ts` — wire `registerOTel()` via `@quilty/observability` factory per **D129 OTel single-target rule** (Sentry consumes OTel spans; one OpenTelemetry export target only)
  - `apps/web/app/layout.tsx` — `<WebVitalsReporter />` consumer re-wired
  - `apps/web/app/error.tsx` — error-reporter call re-wired
  - `apps/web/app/global-error.tsx` — error-reporter call re-wired
  - `apps/web/lib/flags/features.ts` — wire `FeatureFlagEvaluator` from package
  - `apps/web/vitest.config.ts` — update test-setup path to `@quilty/observability/testing`
  - `apps/web/lib/observability/*` (10 files including `__tests__/*`) — DELETE
- `apps/web/eslint.config.mjs` — remove residual `posthog-js` / `posthog-node` entries from M1's `VENDOR_SDK_IMPORTS` array (PostHog stub no longer referenced)
- `apps/web/composition.*.ts` — wire `analytics`, `errorReporter`, `logger`, `replay`, `featureFlags` via factory wrappers (no direct adapter imports)
- Strip workflow-context comments per META-2 in moved code (ADR/D-number references retained where load-bearing)

**Note on `apps/web/lib/observability/consent.ts`:** The M1 consent.ts module migrates in Commit 8 to `@quilty/consent`, NOT here. This commit only touches the observability subtree of `apps/web/lib/observability/`; `consent.ts` is the singular file that stays in apps/web until Commit 8 picks it up. ESLint shim left in place during the gap.

**Decisions referenced:** D75-D81, D82-D86, D96 (Amplitude/Sentry/CloudWatch adapters), D129 OTel single-target, D67 PHI chokepoint (consumed via `@quilty/security` Sanitizer), META-1 (vendor-agnostic naming), META-2 (comment scope), META-3 (≥85% coverage)

**Per-commit QA:** Pass A: typescript-reviewer + hipaa-csp-reviewer (default-deny consent gate + PHI sanitizer chokepoint architectural seal); Pass B: perf-bundle-reviewer (no production bundle bloat) + accessibility-reviewer (verify replay class constants used in components)

### Commit 6 — `refactor(seo): extract apps/web/lib/seo + components/seo → @quilty/seo utility package`

**Files:**

- `packages/seo/package.json` — `@quilty/seo` (utility — no ports)
- `packages/seo/src/builders/{organization,website,software-application,breadcrumb,medical-web-page,faq-page}.ts`
- `packages/seo/src/builders/metadata-helpers.ts` — `buildOpenGraphMetadata({ ogImage, appleTouchIcon })` + `buildIconMetadata()` so layout consumers don't hand-roll metadata objects (D158)
- `packages/seo/src/components/JsonLd.tsx` — CSP-safe stringify (verified to render without CSP violations under marketing CSP)
- `packages/seo/src/__tests__/*.test.ts` — including a JsonLd CSP-safety test
- `packages/seo/README.md`
- `packages/seo/index.ts` — barrel exports
- **Caller enumeration:**
  - `apps/web/app/layout.tsx` — re-wire SEO/JSON-LD imports
  - `apps/web/app/[locale]/(marketing)/page.tsx` — same
  - `apps/web/app/[locale]/(marketing)/science/page.tsx` — same
  - `apps/web/app/[locale]/(marketing)/for-business/page.tsx` — same
  - `apps/web/app/[locale]/(marketing)/customers/page.tsx` — same
  - Any other `page.tsx` referencing `@/lib/seo/*` — grep + rewire (target: zero `@/lib/seo` imports remaining)
  - `apps/web/lib/seo/*` + `apps/web/components/seo/*` (including `__tests__`) — DELETE

**Decisions referenced:** D80 utility-package classification, D158 SEO metadata helper

**Per-commit QA:** Pass A: typescript-reviewer + seo-meta-reviewer; Pass B: hipaa-csp-reviewer

### Commit 7 — `refactor(content): extract apps/web/lib/content + components/blocks → @quilty/content utility package (Velite config stays root-shim)`

**Files:**

- `packages/content/package.json` — `@quilty/content`
- `packages/content/src/schemas.ts` — Zod block schemas (D24/D29/D65)
- `packages/content/src/components/{BlockRenderer,Hero,ValueProp,FeatureGrid,FAQ,TestimonialQuote,CTABanner}.tsx`
- `packages/content/src/load.ts` — Velite content loader
- `packages/content/src/velite-config.ts` — Velite config body (paths still reference `apps/web/content/` MDX source tree — apps own their content)
- `packages/content/src/__tests__/*.test.ts`
- `packages/content/README.md`
- `packages/content/index.ts` — barrel
- **Velite config shim at workspace root:** `velite.config.ts` (root) becomes a thin re-export — `export { default } from '@quilty/content/velite-config'`. Velite CLI invoked from `apps/web` cwd reads the root config; root config imports the package config. This avoids changing the Velite CLI invocation pattern from M1.
- **MDX source tree stays in `apps/web/content/`:** The `.gitkeep` files in `apps/web/content/{blog,changelog,customers,legal}/` are NOT moved. Content is app-specific; the package exports schemas + loader + components only.
- **Caller enumeration:**
  - `apps/web/app/[locale]/(marketing)/page.tsx` — re-wire to `@quilty/content`
  - All other consumers of `@/lib/content/*` or `@/components/blocks/*` — grep + rewire
  - `apps/web/lib/content/*` + `apps/web/components/blocks/*` (including `__tests__/BlockRenderer.test.tsx`) — DELETE

**Decisions referenced:** D24, D29, D65, D80 utility-package classification

**Per-commit QA:** Pass A: typescript-reviewer + accessibility-reviewer (block components a11y); Pass B: seo-meta-reviewer (JSON-LD shape unchanged)

### Commit 8 — `feat(consent): @quilty/consent hexagonal package skeleton + ConsentStore port + cookie taxonomy types + GPC detector + Banner stub`

**This commit ships the package skeleton + types + ConsentStore port + cookie-taxonomy types + GPC detector domain. The Banner UI ships as a typed-but-renderless STUB; the full UI implementation lands in Commit 22. This split allows the package to land cleanly + consumed by Commit 5's observability factory wrappers (default-deny consent gate) without coupling the banner's WCAG 2.2 AA acceptance criteria to the package-extraction commit.**

**Files:**

- `packages/consent/package.json`
- `packages/consent/src/ports.ts` — D87 ConsentStore + ConsentState type + ConsentIdentifier discriminated union + ConsentStore.migrate() signature (implementation in Commit 23)
- `packages/consent/src/domain/cookie-taxonomy.ts` — D98 5-category taxonomy types + classification helpers (grandfathering migration logic lands in Commit 22)
- `packages/consent/src/domain/gpc-detector.ts` — D100 Sec-GPC reader (used by proxy.ts)
- `packages/consent/src/adapters/in-memory.ts` — production-grade in-memory implementation; not a degenerate test stub. Stand-in until DynamoDB-backed adapter ships per D87 (gated on TF layer).
- `packages/consent/src/adapters/dynamodb.ts` — typed-throwing skeleton (signatures only; `throw new NotImplementedError`); contract tests skip this adapter at M1.5 and run against in-memory only. Activated when DDB table provisions in next-sprint AWS work.
- `packages/consent/src/components/Banner.tsx` — **STUB:** returns `null`; only the typed `BannerProps` interface ships here. Full UI in Commit 22.
- `packages/consent/src/components/PreferencesCenter.tsx` — STUB; full content at Commit 27a.
- **Move (not re-export) of `apps/web/components/legal/GpcHonoredIndicator.tsx` → `packages/consent/src/components/GpcHonoredIndicator.tsx`** (D62 visible CCPA §7025(c)(6) indicator). Update `apps/web/app/[locale]/(marketing)/layout.tsx` to import from `@quilty/consent`.
- **Move M1's `apps/web/lib/observability/consent.ts` → `packages/consent/src/server/cookie-reader.ts`.** Update any caller in apps/web to import from `@quilty/consent`. DELETE the apps/web file.
- `packages/consent/src/__fakes__/consent-store.fake.ts`
- `packages/consent/src/__tests__/consent-store.contract.test.ts` — including a default-deny test: `get(id)` on never-set returns `null` (which downstream Analytics wrapper treats as deny)
- `packages/consent/index.ts` + `packages/consent/src/__fakes__/index.ts` — barrel exports
- `apps/web/proxy.ts` — Sec-GPC detection + cookie-id reader (write of `__Host-quilty_consent` happens in Commit 23 — this commit only reads)
- `apps/web/composition.*.ts` — wire ConsentStore (in-memory adapter)

**Decisions referenced:** D87, D97 (stub here, UI Commit 22), D98 (types here, grandfathering Commit 22), D99, D100 (detector here, write-on-GPC Commit 23), D62, D63 (DDB schema)

**Per-commit QA:** Pass A: typescript-reviewer + hipaa-csp-reviewer (consent gate primitive correctness for downstream observability ports); Pass B: accessibility-reviewer (stub renders nothing — verify no a11y-tree pollution)

### Commit 9 — `feat(email): @quilty/email hexagonal package + SES adapter skeleton + DMARC ramp runbook + M365 BAA verification`

**Files:**

- `packages/email/package.json`
- `packages/email/src/ports.ts` — D88 EmailSender + D95 Result envelope application
- `packages/email/src/domain/sanitize-template-data.ts` — **D67 chokepoint extension (per D67-revision):** factory wrapper calls `@quilty/security`'s Sanitizer recursively on ALL string values in `args.data` (including freeform message bodies), not just top-level keys. Contract test submits a template-data object with a phone-number-shaped value at nested path and asserts the SES adapter receives the redacted form.
- `packages/email/src/adapters/ses.ts` — AWS SES adapter ships as typed-throwing skeleton at M1.5 (signatures only; contract tests skip this adapter and run against in-memory only). Activated when D117 DMARC ramp + SES sandbox lift complete in next-sprint AWS work.
- `packages/email/src/adapters/in-memory.ts` — production-grade in-memory; not a degenerate test stub
- `packages/email/src/templates/` — react-email templates (Pin canonical contact-form-acknowledgement template)
- `packages/email/src/__fakes__/email-sender.fake.ts`
- `packages/email/src/__tests__/email-sender.contract.test.ts` — recursive-sanitizer test + default-deny "no email sent if Sanitizer returns redacted-shape-mismatch" contract
- `packages/email/index.ts` + `packages/email/src/__fakes__/index.ts` — barrel exports
- **`docs/runbook/dmarc-ramp.md` (D117):** 8-week DMARC ramp schedule (p=none → p=quarantine 25% → 50% → 100% → p=reject 25% → 50% → 100% → p=reject + DKIM ADSP strict). Rollback triggers documented. TF apply gated on this runbook landing.
- **`docs/runbook/baa-inventory.md` (D169 placeholder):** M1.5 BAA verification checklist for Sentry (D165 explicit-request verification — actionable here, not deferred), M365 (D119 mailbox roster requires M365 BAA in place), AWS BAA (already in place per quilty-aws account state). Sentry BAA verification is a pre-condition for Commit 5 ship-to-prod activation (acknowledged in commit 5 reviewer pass).
- `apps/web/composition.server.ts` — wire EmailSender (in-memory at M1.5; SES post-sandbox-lift)

**Decisions referenced:** D88, D126, D67 chokepoint (recursive), D95 Result envelope, D117 DMARC ramp, D119 mailbox roster prerequisite, D165 Sentry BAA, D169 BAA inventory

**Per-commit QA:** Pass A: typescript-reviewer + hipaa-csp-reviewer (recursive template sanitizer correctness); Pass B: sst-iac-reviewer (SES adapter shape vs AWS infra)

### Commit 10 — `feat(captcha): @quilty/captcha hexagonal package + Turnstile adapter`

**Files:**

- `packages/captcha/package.json`
- `packages/captcha/src/ports.ts` — D89 Captcha
- `packages/captcha/src/adapters/turnstile.ts` — Cloudflare Turnstile adapter
- `packages/captcha/src/adapters/in-memory.ts` — always-OK in dev
- `packages/captcha/src/__fakes__/captcha.fake.ts`
- `packages/captcha/src/__tests__/captcha.contract.test.ts`
- `apps/web/composition.server.ts` — wire Captcha (Turnstile)
- Env var: `TURNSTILE_SECRET_KEY` documented in `.env.example`

**Decisions referenced:** D89, D37

**Per-commit QA:** Pass A: typescript-reviewer + hipaa-csp-reviewer (CSP allowlist for Turnstile CDN); Pass B: perf-bundle-reviewer (Turnstile script weight)

### Commit 11 — `feat(rate-limit): @quilty/rate-limit hexagonal package + DynamoDB adapter skeleton (in-memory production at sprint close)`

**Files:**

- `packages/rate-limit/package.json`
- `packages/rate-limit/src/ports.ts` — D90 RateLimiter
- `packages/rate-limit/src/domain/sliding-window.ts` — sliding-window algorithm logic
- `packages/rate-limit/src/adapters/dynamodb.ts` — typed-throwing skeleton (signatures only; contract tests skip this adapter); activated when DDB table provisions in next-sprint AWS work
- `packages/rate-limit/src/adapters/in-memory.ts` — production-grade in-memory adapter (not a degenerate test stub)
- `packages/rate-limit/src/__fakes__/rate-limiter.fake.ts`
- `packages/rate-limit/src/__tests__/rate-limiter.contract.test.ts` — META-3 ≥85% coverage; rate-limit qualifies as load-bearing (auth-adjacent paths) → target ≥95%
- `packages/rate-limit/index.ts` + `packages/rate-limit/src/__fakes__/index.ts` — barrel exports
- `apps/web/composition.server.ts` — wire RateLimiter (in-memory adapter)

**Decisions referenced:** D90, META-3 (≥95% target for load-bearing package)

**Per-commit QA:** Pass A: typescript-reviewer + hipaa-csp-reviewer; Pass B: perf-bundle-reviewer

---

## Wave 2 — Production bug fixes (5 commits)

> 5 bugs M1 shipped that would break in production.

### Commit 12 — `fix(web): production bug A3.1 — manifest icons + apple-touch-icon + OG default image (placeholder art)`

**Files:**

- `apps/web/public/icon-16.png` + `icon-32.png` + `icon-96.png` + `icon-192.png` + `icon-512.png` + `apple-touch-icon-180.png` + `favicon.ico` + `mask-icon.svg` (placeholder gradient + "Q" glyph; brand-final at M3)
- `apps/web/public/og-default.jpg` (1200×630, ≤1MB, brand gradient + wordmark placeholder)
- `apps/web/app/manifest.ts` — verify icon paths resolve
- `apps/web/app/layout.tsx` — `openGraph.images = ['/og-default.jpg']`

**Decisions referenced:** A3.1, D108, D109

**Per-commit QA:** Pass A: seo-meta-reviewer + accessibility-reviewer; Pass B: perf-bundle-reviewer (image size budgets)

### Commit 13 — `fix(web): production bug A3.2/A3.3 — reconcile AASA paths/components + empty components array`

**Files:**

- `apps/web/public/.well-known/apple-app-site-association` — modern `components` form only (drop legacy `paths`); empty `applinks.details[].components: []` at M1.5 (claim paths added when routes ship per A3.3)
- `apps/web/public/.well-known/assetlinks.json` — verify Android intent filters match mobile SHA256 fingerprint (per A1.3 mobile-team coordination)
- `apps/web/tests/playwright/smoke/wellknown.spec.ts` — assert valid JSON; assert empty components array; assert Apple AASA validator regex passes

**Decisions referenced:** A1.3, A3.2, A3.3

**Per-commit QA:** Pass A: typescript-reviewer + seo-meta-reviewer; Pass B: accessibility-reviewer

### Commit 14 — `fix(infra): sst.config.ts tag alignment + CloudWatch 6yr log retention (HIPAA §164.530(j)) + log-retention runbook`

**Files:**

- `sst.config.ts` — emit canonical mandatory tag set (`quilty:environment`, `quilty:service`, `quilty:owner`, `quilty:stack`, `quilty:repo`, `quilty:cost-center`, etc.) per AWS Org Tag Policy
- `sst.config.ts` — **set `logRetention: 6 years` (2191 days) on Lambda functions + log groups (D127):** HIPAA §164.530(j) audit-log retention requirement. Default 14-day Lambda retention fails HIPAA audit.
- **`docs/runbook/log-retention.md` (D127 + D176):** documents what gets logged at the 6-year tier (hashed admin metadata only, never PHI, never clear-text emails); rationale per Art 17(3)(b) "legal obligation" exception + HIPAA §164.530(j); rotation + archival policy.

**Decisions referenced:** A3.4, A.3 production bugs, D127, D176

**Per-commit QA:** Pass A: sst-iac-reviewer + typescript-reviewer; Pass B: hipaa-csp-reviewer (tag schema doesn't accidentally include PHI fields + 6yr retention doesn't include PHI streams)

### Commit 15 — `fix(a11y): error.tsx + global-error.tsx WCAG SC 4.1.3 — role + aria-live + aria-atomic + focus-on-mount`

**Files:**

- `apps/web/app/error.tsx` — add `role="alert"` + `aria-live="assertive"` + `aria-atomic="true"` + `useRef` + `focus()` on heading
- `apps/web/app/global-error.tsx` — same
- `apps/web/tests/playwright/a11y/error-boundaries.spec.ts` — new test: axe-core verifies aria-live on error boundary mount

**Decisions referenced:** D144 (newly-surfaced production bug)

**Per-commit QA:** Pass A: accessibility-reviewer + typescript-reviewer; Pass B: hipaa-csp-reviewer (digest visibility per D141 preserved)

### Commit 16 — `docs(strategy): revise D9 / D11 / D45 / D52 / D63 / D67 / D68 wording per audit findings`

**Files:**

- `docs/website_strategy_discussion.md` — inline revisions:
  - D9: `quilty.auth.sessions_revoked` → `quilty-{env}-auth-events` with detail-type discrimination + `quilty.auth.Envelope` schema
  - D11: `cognito_sub` → `quilty_sub` (Rust-backend-issued UUID, swap-resilient)
  - D45: `.com` public-facing + `.app` internal/interim with mobile migration plan
  - D52: web 8h vs mobile 30d refresh-token TTL clarification
  - D63: ConsentState DynamoDB schema update
  - D67: PHI sanitizer chokepoint extends to EmailSender.sendTransactional payload
  - D68: per-config replay discipline (not per-vendor blanket)
- Add audit Round-6 reference + brief summary in update log section

**Decisions referenced:** A3.5, B.5 strategy doc revisions

**Per-commit QA:** Pass A: typescript-reviewer (doc consistency) + hipaa-csp-reviewer (D67 extension correctness); Pass B: sst-iac-reviewer (D9 + D11 + D52 align with AWS state)

---

## Wave 3 — Technical routes + discoverability (5 commits)

### Commit 17 — `feat(web): /.well-known/security.txt (RFC 9116) + /security page + CI expires-gate`

**Files:**

- `apps/web/public/.well-known/security.txt` (static plain text)
- `apps/web/app/[locale]/(marketing)/security/page.tsx` (one-paragraph "how to report" page)
- `scripts/check-security-txt-expires.mjs` — fails CI if `(Expires - now) < 30 days`
- `package.json` — `pnpm verify` includes the script
- `.github/workflows/ci.yml` — hygiene job invokes it

**Decisions referenced:** D105, D112

**Per-commit QA:** Pass A: typescript-reviewer + hipaa-csp-reviewer (no PHI in security.txt content); Pass B: sst-iac-reviewer

### Commit 18 — `feat(web): /.well-known/change-password redirect (RFC 8615)`

**Files:**

- `apps/web/next.config.ts` redirects array — `/.well-known/change-password → /account/security` (HTTP 302)

**Decisions referenced:** D106

**Per-commit QA:** Pass A: typescript-reviewer + accessibility-reviewer; Pass B: seo-meta-reviewer

### Commit 19 — `feat(web): /.well-known/gpc.json policy publication`

**Files:**

- `apps/web/public/.well-known/gpc.json` (static `{"gpc": true, "version": 1, "lastUpdate": "<ISO>"}`)
- `apps/web/tests/playwright/seo/gpc-json.spec.ts` — assert valid JSON + correct shape

**Decisions referenced:** D107, D62 + D100 (GPC honored)

**Per-commit QA:** Pass A: typescript-reviewer + hipaa-csp-reviewer; Pass B: seo-meta-reviewer

### Commit 20 — `feat(web): manifest depth (id + scope + categories + display_override + lang + dir + screenshots + shortcuts)`

**Files:**

- `apps/web/app/manifest.ts` — full PWA manifest per D110

**Decisions referenced:** D110

**Per-commit QA:** Pass A: typescript-reviewer + seo-meta-reviewer; Pass B: accessibility-reviewer

### Commit 21 — `feat(seo): robots.ts refinement — Applebot/Applebot-Extended distinction + Cloudflare Content-Signal header + user-initiated AI fetcher allowlist`

**Files:**

- `apps/web/app/robots.ts` — D111 + D156 amendments:
  - ALLOW `Applebot` (Spotlight + Siri), DISALLOW `Applebot-Extended` (AI training)
  - **Cloudflare Content-Signal directive** emitted as `User-agent: <bot>` blocks within `robots.txt` text (per Cloudflare's robots.txt extension, not a response header). Format: `Content-Signal: search=yes, train=no, ai-input=yes` after each crawler block.
  - Explicit allowlist for user-initiated AI fetchers (OAI-SearchBot, Claude-Web/Claude-User, PerplexityBot — separate from training crawlers)
- `apps/web/tests/playwright/seo/robots.spec.ts` — update test cases for new distinctions

**Decisions referenced:** D111, D156

**Per-commit QA:** Pass A: typescript-reviewer + seo-meta-reviewer; Pass B: hipaa-csp-reviewer (no AI-training crawler can index)

---

## Wave 4 — Legal + compliance pages (7 commits)

### Commit 22 — `feat(consent): native cookie banner UI implementation + taxonomy v1 grandfathering logic`

**Files:**

- `packages/consent/src/components/Banner.tsx` — implement full UI replacing Commit 8 stub. **Banner contract spec:**
  - `role="region"` with `aria-label="Cookie consent"` (per WAI guidance: cookie banner is a region not a modal; no focus trap)
  - Focus moves into banner on mount; Escape dismisses with positive consent state preserved (no choice = essential-only); focus returns to body landmark after dismissal
  - CSS uses `env(safe-area-inset-bottom)` so iOS Safari home-indicator users see full banner (D163)
  - "Accept All" / "Reject All" / "Customize" tri-button pattern (no dark-pattern asymmetry — both Accept and Reject equally weighted per CCPA §7004(a)(2))
  - `aria-describedby` points to a short policy-link paragraph
  - Suppressed when `Sec-GPC: 1` detected (per D62 — GPC visible indicator replaces banner)
- `packages/consent/src/components/Banner.test.tsx` — axe-core compliance + keyboard interaction (Tab/Shift-Tab/Enter/Escape) + GPC suppression test + safe-area-inset-bottom rendering test
- `packages/consent/src/domain/cookie-taxonomy.ts` — fill in D98 v1 grandfathering migration logic (existing M1 cookies → new taxonomy bucket mapping)
- `apps/web/app/[locale]/(marketing)/layout.tsx` — render `<Banner />` (suspense-wrapped per existing GpcHonoredIndicator pattern)
- `apps/web/composition.client.ts` — wire client-side ConsentStore reader

**Decisions referenced:** D97, D98 (grandfathering), D104, D163 (safe-area-inset-bottom)

**Per-commit QA:** Pass A: accessibility-reviewer + hipaa-csp-reviewer; Pass B: typescript-reviewer

### Commit 23 — `feat(consent): GPC FORCE-OFF server-side mirror + ConsentStore migrate(cookie→user)`

**Files:**

- `apps/web/proxy.ts` — detect `Sec-GPC: 1` → write `__Host-quilty_consent` cookie → `ConsentStore.set(id, { analytics: false, marketing: false, personalization: false, gpc_honored: true, essential: true })`. **Force-off semantics: GPC does not just flip an indicator; it forces analytics/marketing/personalization to `false`.** Banner is suppressed (per D62 visible indicator replaces banner).
- `packages/consent/src/domain/migrate.ts` — D87 ConsentStore.migrate() implementation
- `apps/web/app/api/auth/callback/route.ts` — call ConsentStore.migrate(cookie → user) on sign-in (placeholder for future auth wire)
- **Contract test addition** in `packages/consent/src/__tests__/consent-store.contract.test.ts`: when state has `analytics: false`, the wrapped `Analytics.track()` from `@quilty/observability` no-ops (validates end-to-end GPC→observability default-deny chain across packages)

**Decisions referenced:** D100 (FORCE-OFF semantics), D87, D62

**Per-commit QA:** Pass A: hipaa-csp-reviewer + typescript-reviewer; Pass B: accessibility-reviewer

### Commit 24 — `feat(legal): Accessibility Statement at /legal/accessibility + alias /accessibility (EAA-compliant)`

**Files:**

- `apps/web/app/[locale]/(marketing)/legal/accessibility/page.tsx` — ~500 words: WCAG 2.2 AA self-asserted, last-reviewed date, 15-business-day feedback SLA, `accessibility@my-quilty.com` mailbox, EAA / EN 301 549 acknowledgment line
- `apps/web/next.config.ts` — `/accessibility` alias to `/legal/accessibility`
- `apps/web/tests/playwright/a11y/accessibility-statement.spec.ts` — axe-core compliance on the page itself

**Decisions referenced:** D101

**Per-commit QA:** Pass A: accessibility-reviewer + seo-meta-reviewer; Pass B: hipaa-csp-reviewer

### Commit 25 — `feat(legal): Sub-processor list at /legal/subprocessors (Stripe 4-column format) + vendor-erasure-orchestration matrix + per-vendor BAA disclosure`

**Files:**

- `apps/web/app/[locale]/(marketing)/legal/subprocessors/page.tsx` — Stripe 4-column table extended to 5 columns (Name / Data Processed / Purpose / Country / **BAA status**). **Email-subscription opt-in form ships disabled at Commit 25** (renders `<button disabled>` with note "Email notifications launching at M3") — the canonical-form-pattern wiring lands at Commit 32 (CSRF + honeypot + Turnstile + Sanitizer + double-opt-in token + ConsentStore.set marketing=true). When Commit 32 lands, the disabled-button is enabled in a follow-up.
- Initial sub-processor list per D102, with per-vendor BAA status disclosed:
  - AWS (BAA in place; quilty-aws production account)
  - Amplitude (BAA pending — Enterprise tier upgrade prerequisite for production; events sanitized at boundary per D67 until upgrade)
  - Sentry (BAA pending — explicit-request verification scheduled per D165; events sanitized at boundary per D67)
  - Cloudflare Turnstile (no BAA required — bot defense, no PHI in scope)
  - M365 (BAA in place — verified per D119 mailbox roster prerequisite)
- 30-day notice rule documented in page footer
- **`docs/compliance/vendor-erasure-matrix.md` (D175):** per-vendor erasure-orchestration matrix — for each sub-processor: which API to call for Art 17 erasure, which fields to redact in deletion, retention exception per Art 17(3)(b). Read by the Rust orchestrator at Commit 27a's DSAR page placeholder.
- `apps/web/tests/playwright/a11y/subprocessors.spec.ts` — axe-core on the page

**Decisions referenced:** D102, D165 BAA disclosure, D169 BAA inventory, D175 vendor erasure matrix

**Per-commit QA:** Pass A: typescript-reviewer + accessibility-reviewer; Pass B: hipaa-csp-reviewer (subprocessor data classification + BAA disclosure accuracy)

### Commit 26 — `feat(legal): /trust path one-page summary (Stripe + Sentry pattern)`

**Files:**

- `apps/web/app/[locale]/(marketing)/trust/page.tsx` — one-page: **HIPAA-aligned** posture (NEVER "HIPAA-compliant"), SOC 2 in-progress status (placeholder), sub-processors link, security-disclosure link (security.txt), incident history link (status page when activated), encryption details high level
- `apps/web/tests/playwright/seo/trust-page.spec.ts` — basic reachability
- `apps/web/tests/playwright/a11y/trust-page.spec.ts` — axe-core compliance

**Decisions referenced:** D103, D104 wording discipline

**Per-commit QA:** Pass A: typescript-reviewer + seo-meta-reviewer; Pass B: hipaa-csp-reviewer + accessibility-reviewer

### Commit 27a — `feat(legal): DSAR entry points — /legal/privacy-choices + /account/privacy (Art 16 + Art 17 + Art 20 + Art 21 affordances)`

**Files:**

- `apps/web/app/[locale]/(marketing)/legal/privacy-choices/page.tsx` — D99 preferences hub. Explicit affordances for GDPR Art 16 (Rectification — "update your data" link to /account/privacy) + Art 21 (Objection — "opt out of profiling" toggle wired to ConsentStore) per D177.
- `apps/web/app/[locale]/(account)/account/privacy/page.tsx` — signed-in self-serve export + delete + rectification UI. Triggers Rust backend `Erase` API placeholder per D134 + `Export` API placeholder per D135.
- `apps/web/tests/playwright/a11y/legal-pages.spec.ts` — axe-core on both pages

**Decisions referenced:** D99, D134, D135, D177 (Art 16/21 UX)

**Per-commit QA:** Pass A: hipaa-csp-reviewer + accessibility-reviewer; Pass B: seo-meta-reviewer

### Commit 27b — `feat(legal): WA MHMDA standalone Consumer Health Data Privacy Policy + Catch-22 disclosure`

**Files:**

- `apps/web/app/[locale]/(marketing)/legal/consumer-health-data-privacy/page.tsx` — D150 WA MHMDA standalone policy (required even if main Privacy Policy covers it; WA MHMDA §19.373.030 mandates standalone). Includes D154 MHMDA Catch-22 disclosure (the WA-specific notice that consumer health data sharing without explicit consent is prohibited even for service-provider routing).
- `apps/web/tests/playwright/a11y/mhmda-page.spec.ts` — axe-core on the page
- Placeholder content marked for M8 lawyer review

**Decisions referenced:** D150, D154

**Per-commit QA:** Pass A: hipaa-csp-reviewer + accessibility-reviewer; Pass B: seo-meta-reviewer

### Commit 27c — `feat(legal): main Privacy Policy stub — Privacy Lead title + identity-verification + jurisdiction SLA + sensitive-data class`

**Files:**

- `apps/web/app/[locale]/(marketing)/legal/privacy/page.tsx` — main Privacy Policy stub. **Privacy Lead title (D149/D136) — NEVER "DPO" per GDPR Art 38(6) fineable risk.** Includes:
  - Identity-verification policy (D151) — what proof is required for a DSAR
  - Per-jurisdiction SLA matrix (D152) — single SLA published externally (45 days, the floor); internal granular matrix at `quilty-aws/docs/compliance/`
  - Sensitive-data-class language (D153) — explicit notice that consumer health data is sensitive under CCPA §1798.140 + WA MHMDA + similar
- Placeholder content marked for M8 lawyer review
- `apps/web/tests/playwright/a11y/privacy-policy.spec.ts` — axe-core on the page

**Decisions referenced:** D136, D149, D151, D152, D153, D155

**Per-commit QA:** Pass A: hipaa-csp-reviewer + accessibility-reviewer; Pass B: seo-meta-reviewer

### Commit 28 — `feat(eslint): NEVER claim "HIPAA-compliant" custom rule (.ts/.tsx/.md/.mdx) + DPO-as-error + sweep`

**Files:**

- `eslint.config.mjs` — custom rule banning case-insensitive `HIPAA-compliant` in `.ts` + `.tsx` + `.md` + `.mdx` files outside `docs/research/_raw/`. **`.ts` inclusion** ensures server-side error strings, API response text, analytics event-property names — anything reaching users via runtime — are covered (Cerebral lesson).
- Sweep all existing README files + CLAUDE.md + strategy doc + new legal pages + ALL `.ts`/`.tsx` source files — replace any "HIPAA-compliant" with "HIPAA-aligned"
- ESLint rule extension: **error** (not warn) on literal `"DPO"` outside `docs/research/_raw/` + the legal/privacy page content directories (Privacy Lead title is firm per D149 GDPR Art 38(6) fineable risk). Allow `DPO` in conditional explanatory text on legal/privacy pages where the page itself disclaims the DPO designation.

**Decisions referenced:** D104, D136, D149

**Per-commit QA:** Pass A: typescript-reviewer + hipaa-csp-reviewer; Pass B: seo-meta-reviewer

---

## Wave 5 — Forms + error handling (4 commits)

### Commit 29 — `feat(web): per-route-group error.tsx + loading.tsx + 410/451/503 status code handling`

**Files:** (6 net files — dropped `api/auth/error.tsx` no-op since Route Handlers don't invoke React error boundaries; dropped `(content)/loading.tsx` since `(content)` route group is M2+ scope)

- `apps/web/app/[locale]/(marketing)/error.tsx` — marketing-tier branded 500 with retry + return-to-home. **Retry-fallback discipline (D147):** if `reset()` is called twice within 5s and the page still fails, render a permanent return-to-home CTA instead of retry. Preserves Commit 15's `role="alert"` + `aria-live="assertive"` + `aria-atomic="true"` + focus-on-mount additions.
- `apps/web/app/[locale]/(marketing)/loading.tsx` — marketing skeleton
- `apps/web/app/[locale]/(account)/error.tsx` — portal-tier preserved signed-in chrome + inline support link + same retry-fallback discipline
- `apps/web/app/[locale]/(account)/account/loading.tsx` — portal shimmer skeleton
- `apps/web/app/[locale]/(marketing)/legal/loading.tsx`
- `apps/web/proxy.ts` — 410 / 451 / 503 status code routing (allowlists + maintenance env var)
- `apps/web/components/site/StatusPage.tsx` — branded 410 / 451 / 503 component (lives in apps/web per wrap-don't-edit; not a package)

**Decisions referenced:** D114, D115, D147 retry-fallback

**Per-commit QA:** Pass A: accessibility-reviewer + typescript-reviewer; Pass B: hipaa-csp-reviewer

### Commit 30 — `feat(web): error.tsx copy-reference-button + mailto support CTA + robots noindex meta + cookie banner suppression`

**Files:**

- `apps/web/app/[locale]/(marketing)/error.tsx` + `apps/web/app/error.tsx` — add "Copy Reference" button next to `error.digest`; add `mailto:support@my-quilty.com?subject=Error+<digest>` CTA; add `<meta name="robots" content="noindex">`; suppress cookie banner via prop on layout
- `apps/web/tests/playwright/smoke/error-page.spec.ts` — assert copy button + mailto CTA + noindex

**Decisions referenced:** D145, D146

**Per-commit QA:** Pass A: accessibility-reviewer + typescript-reviewer; Pass B: seo-meta-reviewer (noindex correctness)

### Commit 31 — `feat(eslint): PHI-in-error-message custom rule`

**Files:**

- `eslint.config.mjs` — custom rule: block `throw new Error(\`...\${variable}...\`)` template-literal patterns where the variable matches D67 PHI denylist names (`email`, `phone`, `dob`, etc.) or shape-matches PHI patterns
- `apps/web/tests/eslint/no-phi-in-error-message.test.ts` — unit tests for the rule

**Decisions referenced:** D148

**Per-commit QA:** Pass A: typescript-reviewer + hipaa-csp-reviewer; Pass B: perf-bundle-reviewer

### Commit 32 — `feat(forms): canonical 7-piece pattern + /contact form (fills Commit 4 stubs)`

**Files:**

- `apps/web/app/[locale]/(marketing)/contact/page.tsx` — replace stub
- `apps/web/app/[locale]/(marketing)/contact/actions.ts` — Server Action: CSRF triple-layer → honeypot + time-trap → Turnstile verify → Zod parse → **`Sanitizer.scrub()` recursively on all string values** (D67 chokepoint) → EmailSender.sendTransactional → `Result<TSuccess, TFormError>` (D95)
- `apps/web/app/[locale]/(marketing)/contact/ContactForm.tsx` — RHF + Zod + shadcn `<Field>` wrap + ARIA-live error region
- **`packages/security/src/domain/csrf.ts`** — fill Commit 4 stub with D10 + D53 triple-layer (Origin/Referer + signed double-submit + custom `X-Quilty-CSRF` header)
- **`packages/security/src/domain/honeypot.ts`** — fill Commit 4 stub with D113 honeypot field generator + verifier
- **`packages/security/src/domain/time-trap.ts`** — fill Commit 4 stub with D113 time-trap helper
- `packages/security/index.ts` — barrel updated to re-export the three filled domain utilities
- `packages/email/src/templates/contact-acknowledgement.tsx` — react-email template
- Wire `@quilty/captcha` Turnstile script-tag in layout (with CSP allowlist for `https://challenges.cloudflare.com` per `@quilty/security`'s CspBuilder)
- `apps/web/tests/playwright/forms/contact.spec.ts` — end-to-end submission test (honeypot blocks bot fill, Turnstile blocks no-token, sanitizer redacts phone-number-shaped value in message)
- `apps/web/tests/playwright/a11y/contact-form.spec.ts` — axe-core compliance + keyboard interaction + ARIA-live error announcement test
- **Sanitizer contract test addition in `packages/email/src/__tests__/email-sender.contract.test.ts`:** submit a contact-acknowledgement template where `args.data.message` contains `Phone: 555-123-4567`; assert the SES adapter (in-memory at M1.5) receives `message: "[REDACTED]"` or equivalent shape.

**Decisions referenced:** D113 (7-piece canonical pattern), D89 Captcha, D10 + D53 CSRF triple-layer, D67 recursive sanitizer on submission payload, D95 Result envelope

**Per-commit QA:** Pass A: typescript-reviewer + accessibility-reviewer; Pass B: hipaa-csp-reviewer (Turnstile + form CSP allowlist + recursive sanitizer chokepoint)

---

## Wave 6 — Cleanup + ADRs + final QA + verification (8 commits)

### Commit 33 — `feat(observability): expand AccountDeleteReason enum to 8 values (D137)`

**Background:** Commit 5 shipped the enum with the M1 baseline 6 values (preserved atomicity of the extraction). This commit performs the D137 expansion: adds `taking_break` + `missing_features`, renames `unspecified` → `other_not_specified`.

**Files:**

- `packages/observability/src/ports.ts` — `AccountDeleteReason` enum: add 2 new values + rename 1 per D137
- `packages/observability/src/__tests__/*.contract.test.ts` — update test fixtures covering all 8 values
- Strategy doc revision logged for the Wave 6 batch update

**Decisions referenced:** D137, D82

**Per-commit QA:** Pass A: typescript-reviewer + hipaa-csp-reviewer; Pass B: perf-bundle-reviewer

### Commit 34 — `chore(monitoring): Sentry Uptime monitor + Sentry Crons + oncall trigger runbook + SEV taxonomy`

**Files:**

- `apps/web/app/api/health/route.ts` — simple JSON health endpoint for Sentry Cron to poll
- `docs/runbook/sentry-monitors.md` — runbook for creating Sentry Uptime monitor (apex) + Sentry Cron (health-endpoint) in Sentry dashboard. NOT TF-codified at M1.5 (Sentry monitors are dashboard-config, not IaC).
- **`docs/runbook/oncall-trigger.md` (D125):** solo-team incident response posture. Documents (a) the current state ("alerts route to founder phone"), (b) the trigger condition for a formal on-call rotation (2nd engineer hired), (c) the SEV1 = 60-day OCR notification window for HIPAA-aligned site, (d) the escalation playbook for the solo phase.
- **`docs/runbook/incidents/sev-taxonomy.md` (D130):** 4-tier SEV classification (SEV1: PHI exposure or full-site outage / SEV2: significant feature breakage / SEV3: degraded UX / SEV4: minor cosmetic). Each SEV has owner + response time + escalation rules. Referenced by D124 Sentry Uptime alerts.
- Optional: Sentry MCP-driven monitor creation script (if MCP supports it)

**Decisions referenced:** D124, D125 on-call trigger, D130 SEV taxonomy

**Per-commit QA:** Pass A: hipaa-csp-reviewer + typescript-reviewer; Pass B: sst-iac-reviewer

### Commit 35 — `chore: META-2 comment-scope sweep + CI grep gate against workflow context`

**Files:**

- Sweep all `.tsx` + `.ts` files in `packages/*/src/` + `apps/web/{app,components,lib}/`: remove any comments referencing "M1", "M1.5", "M2-M9", "Round-5", "Round-6", "Wave-X", "Cluster-Y", "this sprint", "next sprint", "the audit found", agent names — replace with ADR / D-number / RFC reference where the underlying intent matters (META-2). PostHog reference removal was already handled in Commit 5 (observability extraction), so this sweep is purely META-2 hygiene.
- **`scripts/check-no-workflow-context.mjs` (NEW):** grep-based CI gate over `packages/*/src/` + `apps/web/{app,components,lib}/`. Fails CI if any forbidden phrase (case-insensitive: `M1\b`, `M1\.5\b`, `M2-M9\b`, `Round[\s-][56]\b`, `Wave[\s-][0-9]\b`, `Cluster[\s-][0-9]\b`, `this sprint`, `next sprint`, `the audit found`) appears in a code comment. Mirrors Commit 17's security.txt-expires CI gate pattern. Permits the phrases inside content strings deliberately quoting them (rare).
- `package.json` — `pnpm verify` invokes the new script
- `.github/workflows/ci.yml` — hygiene job invokes it

**Decisions referenced:** META-2, META-1

**Per-commit QA:** Pass A: typescript-reviewer (comment scope verification + script correctness) + perf-bundle-reviewer; Pass B: hipaa-csp-reviewer

### Commit 36 — `docs(runbook): note HIPAA officer designation filing in quilty-aws repo`

**Background:** The HIPAA officer designation file itself lives in `quilty-aws/docs/compliance/hipaa-officer-designation.md` (out of this repo's scope). This commit only adds the cross-repo pointer + filing reminder to the post-sprint checklist.

**Files:**

- `docs/runbook/m1.5-post-sprint-checklist.md` — append entry: "File HIPAA officer designation at `quilty-aws/docs/compliance/hipaa-officer-designation.md`. Founder formally designated as both Privacy Officer (§164.530(a)) + Security Officer (§164.308(a)(2)). Retained 6 years per HIPAA §164.530(j)."
- Same checklist file — append BAA inventory spreadsheet creation reminder (D169): "Create `quilty-aws/docs/compliance/baa-inventory.xlsx` with current BAA state for AWS, M365, Sentry, Amplitude (pending). Update on each new sub-processor."

**Decisions referenced:** D139, D155, D169 BAA inventory

**Per-commit QA:** Pass A: hipaa-csp-reviewer; Pass B: typescript-reviewer (runbook prose)

### Commit 37 — `docs(adr): ADR-0008 modular monolith + ADR-0009 hexagonal-by-boundary + ADR-0010 composition root + ADR-0011 naming + ADR-0012 packaging tooling + ADR-0013 forms canonical`

**Files:**

- `docs/adr/0008-modular-monolith.md` — D75 + Nygard format + Turborepo evidence + Cal.com / Next.js / Vercel precedents
- `docs/adr/0009-hexagonal-by-boundary.md` — D76 + 6-agent evidence (reports 12-17) + Cal.com exemplar + Mark Seemann + Stuart-Martin counter-positions + watch-items
- `docs/adr/0010-composition-root.md` — D77 + Sentry Lazar Nikolov precedent + `@evyweb/ioctopus` upgrade target
- `docs/adr/0011-port-adapter-naming.md` — D81 / META-1 + adapter file naming + ESLint chokepoint
- `docs/adr/0012-monorepo-packaging-tooling.md` — META-5 + 5 generators + Cal.com / Plain / Astro / Resend precedents
- `docs/adr/0013-forms-canonical-pattern.md` — D113 7-piece recipe + RHF/Zod/Server Actions + CSRF + honeypot + Turnstile + ARIA

**Decisions referenced:** D75-D81, D113, META-1, META-2, META-5

**Per-commit QA:** Pass A: typescript-reviewer + hipaa-csp-reviewer (ADR text accuracy); Pass B: seo-meta-reviewer (ADR cross-links)

### Commit 38 — `docs(strategy): batch update — add D75-D195 + META-1-5 + cross-references`

**Files:**

- `docs/website_strategy_discussion.md` — comprehensive update:
  - Section "Round 6 — Foundation Audit Locks (2026-05-19/20)" — D75-D195 organized by sub-section (architecture / ports / adapters / cross-cutting / open questions / strategy revisions)
  - Inline revisions to D6/D9/D11/D45/D52/D63/D67/D68 (per Wave 2 commit 16)
  - META-1 through META-5 added in dedicated "Project conventions" subsection
  - Update log entry summarizing Round 6
- `docs/website_workflow_roadmap.md` — update milestones: M2 = App Links meta + content first pages; M3 = Customer.io + USPTO + brand identity + entity formation; M6 = Cognito (deferred trigger); M7 = Stripe + Apple Pay; M8 = launch gate + breach runbook + lawyer review; M9+ = SOC 2 + status page subdomain migration

**Decisions referenced:** ALL of Round 6 (D75-D195 + META-1-5)

**Per-commit QA:** Pass A: typescript-reviewer + hipaa-csp-reviewer (doc internal consistency); Pass B: seo-meta-reviewer (doc cross-references)

### Commit 39 — `chore: round 6 final-QA sweep fixes`

**Files:** depends on 6-agent final QA sweep findings. Anticipate ~10-30 fixes across HIGH + MEDIUM severity findings.

**Pre-conditions:** Final 6-agent QA sweep run (typescript + a11y + hipaa-csp + perf-bundle + seo-meta + sst-iac) across full M1.5 commit range. Synthesize findings into a single fix commit.

**Per-commit QA:** Same shape as M1 final-QA fix commit — Pass A (2 reviewers) → fix → Pass B (1 reviewer) → fix → commit

### Commit 40 — `docs: M1.5 verification report + post-sprint manual-action runbook`

**Files:**

- `docs/m1.5_verification_report.md` — mirror M1 verification report shape; commit-by-commit summary; end-state assertions; per-commit QA evidence; sprint metrics
- `docs/runbook/m1.5_post_sprint_checklist.md` — manual user actions (HIPAA officer designation filing at `quilty-aws/`, CMRA mailbox provisioning, sub-processor email-list provisioning at `legal@my-quilty.com`, M365 mailbox creation coordination per D119, USPTO trademark filing scheduling at M3, Delaware C-Corp formation, sprint-boundary push authorization)

**Decisions referenced:** ALL of M1.5

**Per-commit QA:** Pass A: typescript-reviewer + hipaa-csp-reviewer (report accuracy); Pass B: sst-iac-reviewer (post-sprint checklist completeness)

---

## Final QA — 6-agent parallel sweep before commit 39

After commits 1-38 land locally (note: Wave 4 now has 9 commits due to 27a/27b/27c split; commit numbers 33-40 stay logically named but represent the final eight commits of the 42-commit sprint), launch 6 review agents in parallel:

1. `typescript-reviewer` — full M1.5 commit range; TS strict + RSC boundaries + port shape correctness + default-deny chokepoint architectural seal
2. `accessibility-reviewer` — WCAG 2.2 AA; banner + error pages + forms + legal pages + role/aria-live/aria-atomic on error boundaries
3. `hipaa-csp-reviewer` — PHI sanitizer chokepoint at every port (recursive) + email + consent default-deny + GPC FORCE-OFF + error chokepoint + per-vendor BAA disclosure
4. `perf-bundle-reviewer` — bundle weight after package extraction; size-limit budgets honored
5. `seo-meta-reviewer` — manifest depth + JSON-LD graph + AI crawler policy (Cloudflare Content-Signal) + sitemap
6. `sst-iac-reviewer` — sst.config.ts tags + 6yr CloudWatch log retention + adapter shapes vs AWS-recon state

Synthesize findings → commit 39 fixes them → re-run gates → commit 40 verification report.

---

## Out of scope (deferred to triggers)

| Item                                                    | Trigger                                                     | M-target       |
| ------------------------------------------------------- | ----------------------------------------------------------- | -------------- |
| AWS deploy + DNS records + ACM cert + WAF               | `quilty-aws/website-baseline/` Terraform layer lands        | Next sprint    |
| Cognito custom domain flip                              | DNS apex record exists                                      | Next sprint    |
| SES production-access lift                              | Support case filed                                          | Next sprint    |
| DynamoDB ConsentStore + RateLimiter tables provisioning | TF layer                                                    | Next sprint    |
| Real auth integration (web Cognito BFF)                 | Server cookie-policy + mobile end-to-end utilization        | M6 or later    |
| Real Stripe + Apple Pay + Sign in with Apple            | Stripe integration sprint                                   | M7             |
| Real brand identity (favicons, OG image, voice, copy)   | Brand discovery sprint                                      | M3             |
| Real marketing content (MDX in `apps/web/content/`)     | Content sprint                                              | M2-M4          |
| USPTO trademark filing                                  | Delaware C-Corp formation                                   | M3             |
| BIMI / VMC email branding                               | USPTO trademark complete                                    | Post-M3        |
| HSTS preload submission                                 | Clean window post-launch                                    | M8 launch gate |
| CSP enforce flip (from report-only)                     | Clean window                                                | M8             |
| Trusted Types enforce flip                              | Clean window                                                | M6-M8          |
| Status page activation (Instatus Pro)                   | First incident comm need                                    | M2-M3          |
| Marketing email (Customer.io Premium)                   | Waitlist activation                                         | M3+            |
| Service Worker                                          | PWA install becomes growth lever                            | M9+            |
| SOC 2 Type II readiness                                 | Post-launch ~12 months                                      | M9+            |
| Smart App Banner reconsideration                        | Brand-identity-locked copy + measurable mobile-install need | M9+            |
| Sentry BAA explicit-request verification                | M1.5 implementation time                                    | M1.5 sub-task  |
| External DPO contracting (VeraSafe / DPO Centre)        | First paying EU/UK customer OR first DSAR OR ~5K EU MAU     | Post-M3        |

---

## Verification — how we know M1.5 is done

End-state assertions:

- [ ] `pnpm install --frozen-lockfile` succeeds with new 9-package shape
- [ ] `pnpm typecheck` green across all 10 workspaces (apps/web + 9 packages)
- [ ] `pnpm lint` green
- [ ] `pnpm test` green — unit tests across all 9 packages with ≥85% per-package coverage; load-bearing packages (observability, security, consent, email) ≥95%
- [ ] `pnpm format:check` green
- [ ] `pnpm build` green
- [ ] `pnpm exec playwright test` green on chromium + webkit
- [ ] `pnpm size` budgets honored
- [ ] `pnpm verify` (full hygiene + correctness pipeline) green
- [ ] `pnpm exec turbo gen package <foo>` works (smoke-test the generator)
- [ ] `pnpm exec turbo gen port <pkg> <portname>` works
- [ ] `pnpm exec turbo gen adapter <pkg> <portname> <vendor>` works
- [ ] All 9 packages have parameterized contract tests passing on multiple adapters (real + in-memory)
- [ ] Composition root at `apps/web/composition.{server,client,edge}.ts` is the ONLY place adapter modules are imported
- [ ] dependency-cruiser fails the build if domain → adapter imports introduced
- [ ] ESLint fails the build if vendor SDK imports outside `packages/*/src/adapters/<vendor>.ts`
- [ ] ESLint fails the build if "HIPAA-compliant" string used in `.ts`/`.tsx`/`.md`/`.mdx` outside research archive
- [ ] ESLint fails the build if literal `"DPO"` appears in source outside research archive + opt-in legal-content paths
- [ ] ESLint fails the build if PHI-shape variable interpolated into `Error()` constructor
- [ ] CI fails if security.txt Expires field < 30 days
- [ ] CI fails on axe-core violations
- [ ] CI fails if source files contain workflow-context phrases (M1, M1.5, Round-6, Wave-N, "this sprint", agent names) per META-2 grep gate
- [ ] CloudWatch log retention = 6 years on Lambda functions + log groups per D127
- [ ] Contact form (`/contact`) round-trips through CSRF + honeypot + Turnstile + Server Action + EmailSender (in-memory adapter at M1.5)
- [ ] Cookie banner renders + responds to user choice + persists via ConsentStore (in-memory adapter at M1.5)
- [ ] GPC honored end-to-end (Sec-GPC: 1 → banner skipped → analytics + marketing forced off)
- [ ] Error pages have role="alert" + aria-live="assertive" + aria-atomic="true" + focus on heading
- [ ] All M1 production bugs fixed (manifest icons render, AASA validates, sst.config.ts tags align)
- [ ] All ADRs 0008-0013 committed
- [ ] Strategy doc updated with D75-D195 + META-1-5
- [ ] M1.5 verification report committed
- [ ] Manual user actions documented in `docs/runbook/m1.5_post_sprint_checklist.md`
- [ ] Branch ahead of origin, no push yet (held for user authorization)

---

## Push authorization

Per `feedback_push_per_phase`: M1.5 commits held local on `main` until explicit user authorization at sprint close.

---

## Estimated agent invocations

- Per-commit QA loop: 42 × 3 = 126
- Final 6-agent QA sweep: 6
- **Total: ~132 agent invocations**

Significantly larger than M1 (~36 across 12 commits). The bureaucratic per-commit QA discipline + the additional final sweep are both load-bearing for HIPAA-aligned + multi-vendor stack confidence.

---

## Audit fixes applied (v1 → v2)

This plan was reviewed by 3 audit agents after v1 draft. The following audit findings drove revisions:

**Completeness (Agent 1):** 7 HIGH gaps closed by extending existing commits — D117 DMARC ramp runbook added to Commit 9, D125 oncall trigger + D130 SEV taxonomy added to Commit 34, D127 CloudWatch 6yr log retention + D176 runbook added to Commit 14, D129 OTel single-target wire-up enumerated in Commit 5's caller list, D175 vendor erasure matrix added to Commit 25. PARTIAL findings tightened (D147 retry-fallback, D158 SEO metadata helper, D163 safe-area-inset-bottom, D169 BAA inventory) in their respective commits.

**Sequencing (Agent 2):** 6 HIGH bugs fixed — (1) Commits 4 ↔ 5 swapped so `@quilty/security` Sanitizer port exists before `@quilty/observability` factory wrappers consume it per D67 chokepoint, (2) Commit 1 ESLint/dep-cruiser rules scoped to `packages/**` only so M1 baseline keeps passing, (3) Commit 7 Velite config shim pattern made explicit (root config re-exports from package; MDX source stays in apps/web), (4) Commits 8 + 22 deduplicated — Commit 8 ships Banner stub, Commit 22 fills full UI, (5) Pre-declared CSRF/honeypot/time-trap stubs in Commit 4 so Commit 32 fills (not retroactively expands) the security package surface, (6) M1's `consent.ts` migration explicitly placed in Commit 8 (not Commit 5). Caller enumeration tightened for Commits 5, 6, 7. Commit 27 split into 27a/27b/27c for atomic discipline.

**Compliance (Agent 3):** 6 HIGH violations fixed — (1) Default-deny consent gate semantics now architectural in Commit 5's factory wrappers + contract tests, (2) Commit 23 GPC FORCE-OFF semantics made explicit (analytics/marketing/personalization forced to `false`, not just indicator flag), (3) PHI sanitizer recurses to all string values in EmailSender via Commit 4's Sanitizer + Commit 9's chokepoint extension + Commit 32's contract test, (4) D-number correction (Redirect Validator is D92 not D122), (5) Commit 28 ESLint rule extended to `.ts` files + DPO rule = error, (6) Commit 25 sub-processor opt-in form ships disabled at M1.5 (canonical pattern wires at Commit 32). META-2 CI grep gate added at Commit 35 closes the comment-scope discipline architecturally. META-2 line-by-line risk scan addressed in Commits 3, 8, 9, 11, 13, 21, 33, 35.

LOW findings (reviewer-pick mismatches, naming nits) deferred to per-commit QA loop judgment.
