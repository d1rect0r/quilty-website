// In-memory adapter for {{ pascalCase portName }} (regenerated by `turbo gen fake`).
//
// Production-grade fallback adapter — apps wire this when the real vendor is
// unavailable. NOT a degenerate test stub. Test fakes with deterministic seed
// data live under src/__fakes__/ and are exported via the
// "@quilty/<role>/testing" subpath.

import type { {{ pascalCase portName }} } from '../ports.js';

export function makeInMemory{{ pascalCase portName }}(): {{ pascalCase portName }} {
  return {
    _placeholder: true,
    // Implement port operations against in-memory state.
  };
}
