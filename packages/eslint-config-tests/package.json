{
  "name": "@quilty/eslint-config-tests",
  "version": "0.1.0",
  "description": "Test-only workspace — verifies the root ESLint config's custom rules (compliance language ban + PHI-in-error denylist) via in-memory linting of fixture snippets.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0 <25.0.0",
    "pnpm": ">=10.0.0 <11.0.0"
  },
  "scripts": {
    "lint": "eslint __tests__ --max-warnings 0",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2",
    "vitest": "2.1.8"
  }
}
