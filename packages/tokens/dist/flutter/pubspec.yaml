name: quilty_tokens
description: >-
  Quilty design tokens for Flutter — Material 3 ThemeExtension subclasses
  emitted from the canonical DTCG JSON sources in @quilty/tokens.
  GENERATED — do not hand-edit.
version: 0.1.0
homepage: https://my-quilty.com
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
