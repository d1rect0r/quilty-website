{
  "name": "@quilty/shared-types",
  "version": "0.1.0",
  "description": "OpenAPI-emitted TypeScript types from the Rust backend. Empty at M1; populated post-M5 via ADR-0003 pipeline (utoipa -> openapi-typescript -> @quilty/api-types -> re-export here).",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'no build at M1' && exit 0",
    "clean": "rm -rf dist .turbo *.tsbuildinfo",
    "lint": "echo 'no lint at M1' && exit 0",
    "test": "echo 'no tests at M1' && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "5.7.2"
  },
  "files": [
    "src"
  ],
  "main": "./src/index.ts",
  "types": "./src/index.ts"
}
