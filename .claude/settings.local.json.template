{
  "_comment": "Template for .claude/settings.local.json — machine-local overrides. Copy to settings.local.json (which is gitignored) and customize. NEVER commit secrets here.",
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Bash(aws sso login:*)",
      "Bash(aws sts get-caller-identity:*)",
      "Bash(aws sso logout:*)"
    ]
  },
  "env": {
    "AWS_PROFILE": "quilty-dev"
  }
}
