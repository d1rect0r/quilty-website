{
  "name": "cf-functions",
  "version": "0.1.0",
  "description": "CloudFront Functions edge-tier harness — proxy.ts mirror surfaces (GPC FORCE-OFF, security headers, X-Robots-Tag defense) for the M3+ edge activation gate. Each CFF is a single-file ES2020 handler that runs in AWS's QuickJS-based JS engine.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0 <25.0.0",
    "pnpm": ">=10.0.0 <11.0.0"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@quilty/consent": "workspace:*",
    "@quilty/security": "workspace:*",
    "@types/node": "22.10.2",
    "typescript": "^5.7.2",
    "vitest": "2.1.8"
  },
  "sideEffects": false
}
